1) Navázání sousedství v EIGRP (nutno nejdøíve nahodit interface):
R(config)# interface s0/1/0 
R(config-if)# ip address adresa routeru maska síte
R(config-if)# clock-rate 128000
R(config-if)# no shutdown

To samé zopakovat na druhém routeru (pochopitelnì zamìnit interface a ip address a clock-rate u tam nebude)

Ted dokonèíme navázání EIGRP sousedství, tím e nakonfigurujeme zákl. EIGRP:
R(config)# router eigrp 1 (císlo 1 znací císlo EIGRP, toto císlo musí mít všechny routery v sítí -> routery musí pouívát stejnı EIGRP
R(config-router)# no auto-summary
R(config-router)# network adresa síte maska síte (maska síte bude naopak -> 255.255.255.0 = 0.0.0.255)



2) Zmìna hodnoty bandwidth na 1 Mbit mezi routery...
R(config)# interface s0/0/0
R(config-if)# bandwidth 1024
R(config-if)# no shutdown

To samé provedete na druhém routeru

3) Zmìna hodnoty hold-time na 5ti násobek na routeru na nìjakém interface
R(config)# interface s0/0/0
R(config-if)# ip hold-time eigrp1 30

4) Propagujte manuálnì sumarizovanou sí 192.168.128.0 /25, propagovat v rámci EIGRP (aby jsme to nemìli lehkéééé)
R(config)# interface s0/0/0
R(config-if)# ip summary-address eigrp 1 192.168.128.0 255.255.255.128

to samé provedeme na další interface (posíláme o téhle cestì informace na všechny cesty z daného routeru)

5) Nakonfigurujte a propagujte statickou cestu k R_ISP (Internet Service Provider)
R(config)# ip route 0.0.0.0 0.0.0.0 exit-interface k routeru
R(config)# router eigrp 1
R(config-router)# redistribute static

6) Napište pøíkaz pomocí kterého zjistíme delay na interface s0/0/1 
R(config)# show interface s0/0/1

7) Napište pøíkaz, kterım zjistíme, které parametry síte se aktuálnì pouívají pro vıpoèet metriky na routeru
R(config)# show ip protocol
